# --- Reverse-proxy tiles (no functions) --------------------
/tiles/osm/*  https://tile.openstreetmap.org/:splat                  200
/tiles/orm/*  https://a.tile.openrailwaymap.org/standard/:splat      200
# (Netlify will proxy these and keep the URL same-origin)

# You can optionally add fallback hosts if you like manual switches later:
# /tiles/orm-b/*  https://b.tile.openrailwaymap.org/standard/:splat  200
# /tiles/orm-c/*  https://c.tile.openrailwaymap.org/standard/:splat  200

# --- Everything else (your SPA) ------------------------------------
/*            /index.html                                            200